/**
 * Disponible Sizes on Grid 
 */
export default interface IGridSizes {
    gridSize?: {
        xs?: boolean | number; // 0px
        sm?: boolean | number; // 600px
        md?: boolean | number; // 900px
        lg?: boolean | number; // 1280px
        xl?: boolean | number; // 1536px
    }
}
