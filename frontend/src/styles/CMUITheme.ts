import { createTheme } from "@mui/material/styles";

const CMUITheme = createTheme({
    palette: {
        primary: {
            main: "#424242",
        },
        secondary: {
            main: "#2e7e62",
        },
    },
});

export default CMUITheme;
